"use client";import{useEffect,useState}from"react";import{Shell}from"@/components/Shell";import{api}from"@/lib/api";type U={email:string;full_name:string;two_factor_enabled:boolean};export default function Page(){const[user,setUser]=useState<U>(),[error,setError]=useState("");useEffect(()=>{api("/me").then(setUser).catch(e=>setError(e.message))},[]);return <Shell title="Perfil">{user&&<div className="tile"><strong>{user.full_name}</strong><p>{user.email}</p><span>Segundo factor: {user.two_factor_enabled?"Activo":"Inactivo"}</span></div>}{error&&<p className="error">{error}</p>}</Shell>}
