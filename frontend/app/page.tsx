"use client";import {FormEvent,useState} from "react";import Link from "next/link";import {useRouter} from "next/navigation";import {api,saveToken} from "@/lib/api";
export default function Login(){const router=useRouter(),[error,setError]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setError("");const f=new FormData(e.currentTarget);try{const r=await api("/auth/login",{method:"POST",body:JSON.stringify({email:f.get("email"),password:f.get("password")})});if(r.requires_two_factor){sessionStorage.setItem("challenge_token",r.challenge_token);router.push("/verify-2fa")}else{saveToken(r.access_token);router.push("/select-tenant")}}catch(x){setError(x instanceof Error?x.message:"Error")}}return <main className="shell"><form className="card" onSubmit={submit}><h1>Bienvenido a Nexus</h1><p>Accede a tus empresas desde un entorno seguro.</p><label>Correo<input name="email" type="email" required autoComplete="email"/></label><label>Contraseña<input name="password" type="password" required autoComplete="current-password"/></label>{error&&<p className="error" role="alert">{error}</p>}<button>Iniciar sesión</button><p>¿Primera vez? <Link href="/register">Crear cuenta</Link></p></form></main>}
