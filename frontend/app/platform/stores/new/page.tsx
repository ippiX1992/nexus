"use client";import{FormEvent,useState}from"react";import{useRouter}from"next/navigation";import{Shell}from"@/components/Shell";import{ApiError}from"@/lib/api";import{createResource,selectStore}from"@/lib/platform";
export default function Page(){const router=useRouter(),[error,setError]=useState(""),[saving,setSaving]=useState(false);async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setSaving(true);const form=new FormData(event.currentTarget);try{const store=await createResource("/stores",{code:form.get("code"),name:form.get("name"),slug:form.get("slug"),default_locale:form.get("locale"),default_currency:form.get("currency"),timezone:form.get("timezone")});selectStore(store.id);router.push(`/platform/stores/${store.id}`)}catch(e){const x=e as ApiError;setError(`${x.message}${x.correlationId?` · ID ${x.correlationId}`:""}`);setSaving(false)}}return <Shell title="Crear store"><form className="tile" onSubmit={submit}><label>Código<input name="code" required/></label><label>Nombre<input name="name" required/></label><label>Slug<input name="slug" required/></label><label>Locale BCP 47<input name="locale" defaultValue="es-EC" required/></label><label>Moneda ISO 4217<input name="currency" defaultValue="USD" maxLength={3} required/></label><label>Zona horaria IANA<input name="timezone" defaultValue="America/Guayaquil" required/></label><button disabled={saving}>{saving?"Creando…":"Crear store"}</button>{error&&<p className="error" role="alert">{error}</p>}</form></Shell>}
