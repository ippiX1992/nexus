"use client";import{useEffect,useState}from"react";import{Shell}from"@/components/Shell";import{api}from"@/lib/api";type M={membership_id:string;email:string;full_name:string;is_active:boolean;roles:string[]};export default function Page(){const[members,setMembers]=useState<M[]>([]),[error,setError]=useState("");useEffect(()=>{api("/members").then(setMembers).catch(e=>setError(e.message))},[]);return <Shell title="Miembros"><p>Personas con acceso a la empresa activa.</p>{members.map(m=><div className="row" key={m.membership_id}><span><strong>{m.full_name}</strong><br/>{m.email}</span><span>{m.roles.join(", ")} · {m.is_active?"Activo":"Inactivo"}</span></div>)}{error&&<p className="error" role="alert">{error}</p>}</Shell>}
