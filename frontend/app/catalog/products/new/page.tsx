"use client";
import{FormEvent,useEffect,useState}from"react";import{useRouter}from"next/navigation";
import{Shell}from"@/components/Shell";
import{Brand,catalogCreate,catalogPage,ProductDetail,ProductType,technicalError}from"@/lib/catalog";
export default function Page(){const router=useRouter(),[types,setTypes]=useState<ProductType[]>([]),[brands,setBrands]=useState<Brand[]>([]),[error,setError]=useState(""),[saving,setSaving]=useState(false);useEffect(()=>{Promise.all([catalogPage<ProductType>("/product-types?status=active"),catalogPage<Brand>("/brands?status=active")]).then(([a,b])=>{setTypes(a.items);setBrands(b.items)}).catch(e=>setError(technicalError(e)))},[]);async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setSaving(true);const form=new FormData(event.currentTarget);try{const product=await catalogCreate<ProductDetail>("/products",{product_type_id:form.get("product_type_id"),brand_id:form.get("brand_id")||null,code:form.get("code")||null,sku:form.get("sku"),translation:{locale:form.get("locale"),name:form.get("name"),slug:form.get("slug"),short_description:null,long_description:null}});router.push(`/catalog/products/${product.product.id}`)}catch(e){setError(technicalError(e));setSaving(false)}}return <Shell title="Crear producto">{types.length===0&&<p className="error">Primero crea un Product Type activo.</p>}<form className="tile" onSubmit={submit}><label>Product Type<select name="product_type_id" required><option value="">Selecciona</option>{types.map(item=><option key={item.id} value={item.id}>{item.name}</option>)}</select></label><label>Brand<select name="brand_id"><option value="">Sin Brand</option>{brands.map(item=><option key={item.id} value={item.id}>{item.name}</option>)}</select></label><label>Código interno<input name="code"/></label><label>SKU default<input name="sku" required/></label><label>Nombre<input name="name" required/></label><label>Slug<input name="slug" required/></label><label>Locale<input name="locale" defaultValue="es-EC" required/></label><button disabled={saving||types.length===0}>{saving?"Creando…":"Crear Product"}</button>{error&&<p className="error" role="alert">{error}</p>}</form></Shell>}
