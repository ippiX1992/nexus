"use client";
import{FormEvent,useEffect,useState}from"react";import{useParams}from"next/navigation";
import{CatalogNav}from"@/components/CatalogNav";import{Shell}from"@/components/Shell";
import{activeStore,listStores,selectStore,Store}from"@/lib/platform";
import{Category,catalogCommand,catalogContext,catalogCreate,catalogGet,catalogPage,catalogUpdate,ProductDetail,Taxonomy,technicalError,Variant}from"@/lib/catalog";

export default function Page(){const params=useParams<{id:string}>(),productId=params.id;const[detail,setDetail]=useState<ProductDetail|null>(null),[permissions,setPermissions]=useState<string[]>([]),[stores,setStores]=useState<Store[]>([]),[storeId,setStoreId]=useState(activeStore()??""),[categories,setCategories]=useState<Category[]>([]),[error,setError]=useState(""),[loading,setLoading]=useState(true);const canUpdate=permissions.includes("catalog.product.update"),canVariant=permissions.includes("catalog.variant.update"),canCreateVariant=permissions.includes("catalog.variant.create"),canArchive=permissions.includes("catalog.product.archive"),canAssign=permissions.includes("catalog.assignment.manage"),canCategory=permissions.includes("catalog.assignment.manage");async function load(){setLoading(true);try{const[value,ctx,storeRows,taxonomyPage]=await Promise.all([catalogGet<ProductDetail>(`/products/${productId}`),catalogContext(),listStores(),catalogPage<Taxonomy>("/taxonomies")]);const categoryRows=(await Promise.all(taxonomyPage.items.map(item=>catalogGet<Category[]>(`/taxonomies/${item.id}/categories`)))).flat();setDetail(value);setPermissions(ctx.permissions);setStores(storeRows);setCategories(categoryRows);setError("")}catch(e){setError(technicalError(e))}finally{setLoading(false)}}useEffect(()=>{load()},[productId]);function chooseStore(id:string){setStoreId(id);if(id)selectStore(id)}async function productAction(action:"activate"|"archive"){if(!detail)return;try{await catalogCommand(`/products/${productId}/${action}`,detail.product.version);await load()}catch(e){setError(technicalError(e))}}async function createVariant(event:FormEvent<HTMLFormElement>){event.preventDefault();const element=event.currentTarget,form=new FormData(element);try{await catalogCreate(`/products/${productId}/variants`,{sku:form.get("sku")});element.reset();await load()}catch(e){setError(technicalError(e))}}async function updateVariant(variant:Variant,sku:string){try{await catalogUpdate(`/variants/${variant.id}`,{sku},variant.version);await load()}catch(e){setError(technicalError(e))}}async function archiveVariant(variant:Variant){try{await catalogCommand(`/variants/${variant.id}/archive`,variant.version);await load()}catch(e){setError(technicalError(e))}}async function assignStore(){if(!storeId)return;try{await catalogCreate(`/products/${productId}/stores/${storeId}`,{status:"draft"},"PUT");await load()}catch(e){setError(technicalError(e))}}async function unassignStore(){const assignment=detail?.stores.find(item=>item.store_id===storeId);if(!assignment)return;try{await catalogCommand(`/products/${productId}/stores/${storeId}`,assignment.version,"DELETE");await load()}catch(e){setError(technicalError(e))}}async function assignCategory(categoryId:string){if(!detail||!categoryId)return;const assignments=detail.categories.filter(item=>item.category_id!==categoryId).map(item=>({category_id:item.category_id,is_primary:item.is_primary,position:item.position}));assignments.push({category_id:categoryId,is_primary:assignments.length===0,position:assignments.length});try{await catalogUpdate(`/products/${productId}/categories`,{assignments},detail.product.version,"PUT");await load()}catch(e){setError(technicalError(e))}}return <Shell title="Product detail"><CatalogNav/>{loading&&!detail?<p>Cargando…</p>:detail&&<><div className="tile"><strong>{detail.translations[0]?.name??detail.product.code??detail.product.id}</strong><p>{detail.product.status} · v{detail.product.version} · activo no significa publicado</p><div className="nav">{canUpdate&&detail.product.status==="draft"&&<button className="compact" onClick={()=>productAction("activate")}>Activar</button>}{canArchive&&detail.product.status!=="archived"&&<button className="compact" onClick={()=>productAction("archive")}>Archivar</button>}<button className="compact" onClick={load}>Recargar</button></div></div><h2>Variants</h2>{canCreateVariant&&detail.product.status!=="archived"&&<form className="tile" onSubmit={createVariant}><label>Nuevo SKU<input name="sku" required/></label><button>Crear Variant</button></form>}{detail.variants.map(variant=><VariantEditor key={variant.id} variant={variant} editable={canVariant&&variant.status!=="archived"} canArchive={permissions.includes("catalog.variant.archive")} onSave={updateVariant} onArchive={archiveVariant}/>) }<h2>Category assignment</h2>{detail.categories.map(item=><p key={item.category_id}>{categories.find(category=>category.id===item.category_id)?.name??item.category_id}{item.is_primary?" · primary":""}</p>)}{canCategory&&detail.product.status!=="archived"&&<label>Agregar Category<select aria-label="Agregar Category" defaultValue="" onChange={event=>assignCategory(event.target.value)}><option value="">Selecciona</option>{categories.filter(item=>item.status!=="archived"&&!detail.categories.some(value=>value.category_id===item.id)).map(item=><option key={item.id} value={item.id}>{item.name}</option>)}</select></label>}<h2>Store assignment</h2><label>Store<select aria-label="Store assignment" value={storeId} onChange={event=>chooseStore(event.target.value)}><option value="">Selecciona</option>{stores.filter(item=>item.status!=="archived").map(item=><option key={item.id} value={item.id}>{item.name}</option>)}</select></label>{storeId&&<p>{detail.stores.find(item=>item.store_id===storeId)?.status??"No asignado"}{detail.stores.find(item=>item.store_id===storeId)?.eligible?" · eligible":" · no eligible"}</p>}{canAssign&&storeId&&detail.product.status!=="archived"&&<div className="nav"><button className="compact" onClick={assignStore}>Asignar Store</button>{detail.stores.some(item=>item.store_id===storeId&&item.status!=="archived")&&<button className="compact" onClick={unassignStore}>Retirar Store</button>}</div>}</>}{error&&<p className="error" role="alert">{error}</p>}</Shell>}

function VariantEditor({variant,editable,canArchive,onSave,onArchive}:{variant:Variant;editable:boolean;canArchive:boolean;onSave:(variant:Variant,sku:string)=>Promise<void>;onArchive:(variant:Variant)=>Promise<void>}){const[sku,setSku]=useState(variant.sku);return <div className="row"><span><strong>{variant.is_default?"Default Variant":"Variant"}</strong><br/>{variant.status} · v{variant.version}</span><span>{editable?<input aria-label={`SKU ${variant.sku}`} value={sku} onChange={event=>setSku(event.target.value)}/>:variant.sku}{editable&&sku!==variant.sku&&<button className="compact" onClick={()=>onSave(variant,sku)}>Guardar SKU</button>}{canArchive&&variant.status!=="archived"&&!variant.is_default&&<button className="compact" onClick={()=>onArchive(variant)}>Archivar</button>}</span></div>}
