"use client";import{useEffect,useState}from"react";import{Shell}from"@/components/Shell";import{api}from"@/lib/api";type R={id:string;name:string;is_system:boolean;permissions:string[]};export default function Page(){const[roles,setRoles]=useState<R[]>([]),[error,setError]=useState("");useEffect(()=>{api("/roles").then(setRoles).catch(e=>setError(e.message))},[]);return <Shell title="Roles y permisos"><div className="grid">{roles.map(r=><div className="tile" key={r.id}><strong>{r.name}</strong><p>{r.is_system?"Rol del sistema":"Rol personalizado"}</p><small>{r.permissions.join(" · ")}</small></div>)}</div>{error&&<p className="error" role="alert">{error}</p>}</Shell>}
