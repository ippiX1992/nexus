// Shared input styling for admin forms — one source of truth so every text
// input, select and textarea in the panel looks identical. The global
// `input,select,textarea { width:100% }` rule already handles width.
export const inputCls = "rounded-lg border border-line bg-bg px-3 py-2 text-sm text-text focus:border-brand focus:outline-none";
